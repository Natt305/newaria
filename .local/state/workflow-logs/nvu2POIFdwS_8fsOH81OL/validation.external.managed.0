VERDICT: APPROVED
The diff correctly implements the three requested prompt-rewriter fixes in both `groq_ai.py` and `ollama_ai.py`, with no blocking issues.

Critical findings / analysis:
- **Eye color rule fixed**: In both backends, the old honey-gVERDICT: APPROVED
old/amber framing was replaced with the expanded olive-hazel guidaThe diff correctly implements the three requested prompt-rewriter fixes in both `groq_ai.py` and `ollama_ai.py`, with no blocking issues.
nce (hue + saturation + undertones, explici
t anti-default to vivid warm amber). This directly addresses theCritical findings / analysis:
 reported color drift.
- **Eyelash rule added**: Both files now include explicit instructions for lash color/weight in light/cool palettes and caution against default dark-heavy lashes, matching the task objective.
- **Bang asymmetry elevated**: A dedicated **BANG ASYMMETRY (CRITICAL)** standalone rule was added and placed early in both prompts. In `groq_ai.py`, it is explicitly inserted before the broader hairstyle block as requested.
- **Good-output examples updated**: `groq_ai.py` example now uses “soft muted olive-hazel…subtle greenish tint” and includes light lashes. `ollama_ai.py` good example was similarly updated to demonstrate the target eye/lash behavior and asymmetry emphasis.
- No structural code changes, logic rewrites, or unrelated edits were introduced; modifications are tightly scoped to instruction text/examples as required.

Security: none observed.

Next actions:
1. Run a quick A/B prompt rewrite sample (same input before/after) on both Groq and Ollama paths to confirm textual outputs now consistently surface bang side, muted olive-hazel eyes, and light lashes.
2. If regression tests exist for prompt rewriting snapshots, update/add fixtures to lock these three rules so future edits don’t revert them.
3. Optionally monitor a small batch of generated images for improved adherence (especially lash darkness and symmetric bangs) to validate downstream model behavior.- **Eye color rule fixed**: In both backends, the old honey-gold/amber framing was replaced with the expanded olive-hazel guidance (hue + saturation + undertones, explicit anti-default to vivid warm amber). This directly addresses the reported color drift.
- **Eyelash rule added**: Both files now include explicit instructions for lash color/weight in light/cool palettes and caution against default dark-heavy lashes, matching the task objective.
- **Bang asymmetry elevated**: A dedicated **BANG ASYMMETRY (CRITICAL)** standalone rule was added and placed early in both prompts. In `groq_ai.py`, it is explicitly inserted before the broader hairstyle block as requested.
- **Good-output examples updated**: `groq_ai.py` example now uses “soft muted olive-hazel…subtle greenish tint” and includes light lashes. `ollama_ai.py` good example was similarly updated to demonstrate the target eye/lash behavior and asymmetry emphasis.
- No structural code changes, logic rewrites, or unrelated edits were introduced; modifications are tightly scoped to instruction text/examples as required.

Security: none observed.

Next actions:
1. Run a quick A/B prompt rewrite sample (same input before/after) on both Groq and Ollama paths to confirm textual outputs now consistently surface bang side, muted olive-hazel eyes, and light lashes.
2. If regression tests exist for prompt rewriting snapshots, update/add fixtures to lock these three rules so future edits don’t revert them.
3. Optionally monitor a small batch of generated images for improved adherence (especially lash darkness and symmetric bangs) to validate downstream model behavior.
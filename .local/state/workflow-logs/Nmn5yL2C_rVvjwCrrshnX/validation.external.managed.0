VERDICT: APPROVED

The diff correctly implements the task objective: both `groq_ai.py` and `ollama_ai.py` now include the new “DISCARD raw prompt appearance words” rule in the system prompt Rules section, positioned immediately before the HAIR COLOVERDICT: APPROVED
R rule as requested. This directly addresses the duplication/ove
rride issue by explicitly forbidding reuse of raw-prompt appearance descriptors and requiring a single authoritative trait description.

Critical findings / analysis:
- **Requirement met in both target files**: The new rule text is present and semantically aligned with the requested behavior (replace-from-reference, do not copy raw appearance words, exactly one trait description).
- **Placement is correct**: Inserted immediately before HAIR COLOR in both files, matching the specification.
- **Bang-gap enforcement improved**: The BANG ASYMMETRY line was also strengthened from “high priority” to “MANDATORY,” including explicit prohibition of omitting visible gap detail. This is consistent with the broader task framing (“enforce bang gap”) and is not a regression.
- **Scope remains prompt-only**: No code-path, logic, or API behavior changes beyond prompt string content.

Security: none observed (prompt text adjustment only; no auth, data handling, or execution-sink changes).

Next actions:
1) Run a quick A/B prompt-output sanity check with representative `[IMAGE:]` markers to verify early duplicated appearance words are no longer echoed.
2) Validate that both Groq and Ollama outputs preserve mandatory bang-gap side detail when present in references.
3) If available, add/refresh a small regression test fixture around `enhance_image_prompt` prompt-template expectations to lock this rule ordering in place.The diff correctly implements the task objective: both `groq_ai.py` and `ollama_ai.py` now include the new “DISCARD raw prompt appearance words” rule in the system prompt Rules section, positioned immediately before the HAIR COLOR rule as requested. This directly addresses the duplication/override issue by explicitly forbidding reuse of raw-prompt appearance descriptors and requiring a single authoritative trait description.

Critical findings / analysis:
- **Requirement met in both target files**: The new rule text is present and semantically aligned with the requested behavior (replace-from-reference, do not copy raw appearance words, exactly one trait description).
- **Placement is correct**: Inserted immediately before HAIR COLOR in both files, matching the specification.
- **Bang-gap enforcement improved**: The BANG ASYMMETRY line was also strengthened from “high priority” to “MANDATORY,” including explicit prohibition of omitting visible gap detail. This is consistent with the broader task framing (“enforce bang gap”) and is not a regression.
- **Scope remains prompt-only**: No code-path, logic, or API behavior changes beyond prompt string content.

Security: none observed (prompt text adjustment only; no auth, data handling, or execution-sink changes).

Next actions:
1) Run a quick A/B prompt-output sanity check with representative `[IMAGE:]` markers to verify early duplicated appearance words are no longer echoed.
2) Validate that both Groq and Ollama outputs preserve mandatory bang-gap side detail when present in references.
3) If available, add/refresh a small regression test fixture around `enhance_image_prompt` prompt-template expectations to lock this rule ordering in place.
VERDICT: APPROVED_WITH_COMMENTS
The diff correctly fixes the core fallback/refusal issues in `groq_ai.py` and adds the context-gated decline heuristic in both backends, but there is one scope gap worth noting around Ollama text-model fallback handling.VERDICT: APPROVED_WITH_COMMENTS


Critical findings / analysis:
- **Bug 1 (text model fallback corruption):** In `groq_ai.chat()`, tThe diff correctly fixes the core fallback/refusal issues in `groq_ai.py` and adds the context-gated decline heuristic in both backends, but there is one scope gap worth noting around Ollama text-model fallback handling.
he implementation now builds both multimodal and text-only message arrays and switche
s to text-only when retrying non-vision models. ThCritical findings / analysis:
is directly addresses the reported corruption case where `image_url` blocks leaked into text-model retries.
- **Bug 2 (novel refusal phrasing not routed):** `response_declines_image()` in both `groq_ai.py` and `ollama_ai.py` now uses a two-pass approach: existing phrase list + context-gated negation regex (`user_wants_image(messages)` + no `[IMAGE:]` marker + negation match). This aligns with the requested behavior and should catch cases like “抱歉，我無法提供”.
- **Bug 3 (self-ref detection for marker third-person prompts):** Not in this commit diff, but current `bot.py` already checks self-reference against both `user_text` and `image_prompt` (`_is_self_ref = ...user_text... or ...image_prompt...`), which satisfies the requirement.

Comment / gap:
- `ollama_ai.py` did **not** add a Groq-style vision→text retry path (or text-only message reconstruction for such retries). If Ollama intentionally has no model retry/fallback path, this is acceptable; if fallback is expected there too, this task is only partially implemented for Ollama.

Security: none observed.

Next actions:
1. Confirm intended Ollama behavior: if vision→text fallback is required, add explicit retry order and text-only message rebuild analogous to `groq_ai`.
2. Add a regression test covering Groq fallback from vision failure to text model with `context_images` present (assert no `image_url` parts are sent).
3. Add tests for refusal heuristic in both backends (image intent + negation + no marker => fallback to image generation).- **Bug 1 (text model fallback corruption):** In `groq_ai.chat()`, the implementation now builds both multimodal and text-only message arrays and switches to text-only when retrying non-vision models. This directly addresses the reported corruption case where `image_url` blocks leaked into text-model retries.
- **Bug 2 (novel refusal phrasing not routed):** `response_declines_image()` in both `groq_ai.py` and `ollama_ai.py` now uses a two-pass approach: existing phrase list + context-gated negation regex (`user_wants_image(messages)` + no `[IMAGE:]` marker + negation match). This aligns with the requested behavior and should catch cases like “抱歉，我無法提供”.
- **Bug 3 (self-ref detection for marker third-person prompts):** Not in this commit diff, but current `bot.py` already checks self-reference against both `user_text` and `image_prompt` (`_is_self_ref = ...user_text... or ...image_prompt...`), which satisfies the requirement.

Comment / gap:
- `ollama_ai.py` did **not** add a Groq-style vision→text retry path (or text-only message reconstruction for such retries). If Ollama intentionally has no model retry/fallback path, this is acceptable; if fallback is expected there too, this task is only partially implemented for Ollama.

Security: none observed.

Next actions:
1. Confirm intended Ollama behavior: if vision→text fallback is required, add explicit retry order and text-only message rebuild analogous to `groq_ai`.
2. Add a regression test covering Groq fallback from vision failure to text model with `context_images` present (assert no `image_url` parts are sent).
3. Add tests for refusal heuristic in both backends (image intent + negation + no marker => fallback to image generation).